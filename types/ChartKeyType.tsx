export type ChartKeyType = {
    color?: string;   
    clickHandler?: () => void;
    title: string;
    borderColor: string;
    texColor: string;
    paddingH: string;
    paddingV: string;
  };
  
